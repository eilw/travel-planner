%h1 Add a new destination option to the trip
.row
  .col-md-4
  .col-md-4
    = simple_form_for @destination do |f|
      = f.input :name
      = f.input :description
      .actions
        = f.submit 'Add destination', class: 'btn btn-primary'
